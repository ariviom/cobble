import '@testing-library/jest-dom';
import React from 'react';

// Make React available globally for components using JSX without explicit import
// (e.g., components with 'use client' directive)
globalThis.React = React;
